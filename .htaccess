<IfModule mod_rewrite.c>
    # Enable Rewrite Engine
    RewriteEngine On

    # Remove X-Powered-By Header for Security
    Header unset X-Powered-By

    # Force SSL (Redirect HTTP to HTTPS)
    RewriteCond %{HTTPS} !=on
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Redirect www to non-www (Optional: Uncomment if you want to enforce non-www)
    RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
    RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

    # Remove public folder from URL
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L,QSA]

</IfModule>

# BEGIN cPanel-generated php ini directives, do not edit
# Manual editing of this file may result in unexpected behavior.
# To make changes to this file, use the cPanel MultiPHP INI Editor (Home >> Software >> MultiPHP INI Editor)
# For more information, read our documentation (https://go.cpanel.net/EA4ModifyINI)
<IfModule php8_module>
    # Disable displaying PHP errors for security
    php_flag display_errors Off

    # Increase PHP execution time and input limits
    php_value max_execution_time 300
    php_value max_input_time 600
    php_value max_input_vars 10000

    # Set memory limit and file upload size
    php_value memory_limit 4G
    php_value post_max_size 500M
    php_value upload_max_filesize 500M

    # Session settings
    php_value session.gc_maxlifetime 14400
    php_value session.save_path "/var/cpanel/php/sessions/ea-php80"

    # Disable output compression (Optional: Enable if needed)
    php_flag zlib.output_compression Off

    # Enable OPcache for better performance
    php_flag opcache.enable On
    php_value opcache.memory_consumption 128
    php_value opcache.max_accelerated_files 10000
</IfModule>

<IfModule lsapi_module>
    # Same settings as above for LiteSpeed PHP module
    php_flag display_errors Off
    php_value max_execution_time 300
    php_value max_input_time 600
    php_value max_input_vars 10000
    php_value memory_limit 4G
    php_value post_max_size 500M
    php_value session.gc_maxlifetime 14400
    php_value session.save_path "/var/cpanel/php/sessions/ea-php80"
    php_value upload_max_filesize 500M
    php_flag zlib.output_compression Off

    # Enable OPcache for LiteSpeed
    php_flag opcache.enable On
    php_value opcache.memory_consumption 128
    php_value opcache.max_accelerated_files 10000
</IfModule>
# END cPanel-generated php ini directives, do not edit

# BEGIN cPanel-generated php ini directives, do not edit
# Manual editing of this file may result in unexpected behavior.
# To make changes to this file, use the cPanel MultiPHP INI Editor (Home >> Software >> MultiPHP INI Editor)
# For more information, read our documentation (https://go.cpanel.net/EA4ModifyINI)
<IfModule php8_module>

</IfModule>
<IfModule lsapi_module>

</IfModule>
# END cPanel-generated php ini directives, do not edit
query_cache_size =256M
query_cache_type = 1
<IfModule mod_brotli.c>
    AddOutputFilterByType BROTLI_COMPRESS text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

